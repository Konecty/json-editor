{
    "title": "String Comparison",
    "id": "string-comparison",
    "type": "object",
    "properties": {
        "$e": {
            "title": "equal",
            "description": "Matches values that are equal to the value specified.",
            "type": "string"
        },
        "$gt": {
            "title": "greater than",
            "description": "Matches values that are greater than the value specified.",
            "type": "string"
        },
        "$gte": {
            "title": "equal to or greater than",
            "description": "Matches values that are equal to or greater than the value specified.",
            "type": "string",
            "required": ["$gte"]
        }
    }
}
