{
    "title": "greater than",
    "id": "comparison-string-gt",
    "type": "object",
    "properties": {
        "$gt": {
            "title": "Value",
            "description": "Matches values that are greater than the value specified.",
            "type": "string",
            "required": ["$gt"]
        }
    }
}
